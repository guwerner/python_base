def areaRectangle(a, b):  # Function to find the area of a rectangle
    return (a * b) 
  
def perimeterRectangle(a, b): # Function to find the perimeter of a rectangle
    return (2 * (a + b)) 
a = 5; 
b = 6; 

print ("Area = ", areaRectangle(a, b))

print ("Perimeter = ", perimeterRectangle(a, b))
